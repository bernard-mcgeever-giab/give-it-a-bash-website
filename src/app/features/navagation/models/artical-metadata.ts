export class ArticalMetadata {
    public loc: string;
    public navIndentation: number;

    constructor(loc: string, navIndentation: number) {
        this.loc = loc;
        this.navIndentation = navIndentation;
    }
}